<div class="container mt-3">
  <div class="row">
    <div class="col-xl-8 col-lg-8 col-md-12 col-sm-12 col-12 d-flex flex-column">
      <div class="mt-5">
        <div>
          <h1 class="site-title">
            NiceWork
          </h1>
        </div>
        <div class="h5">
          <p>
            ユーザーは自分の目標や頑張ったことを投稿し、他のユーザーからの応援やいいねを受けることでモチベーションを高めることができます。
          </p>
        </div>
      </div>
    </div>
    <div class="col-xl-4 col-lg-4 col-md-12 col-sm-12 col-12 d-flex flex-column">
      <% if user_signed_in? %>
        <div class="mb-2">
          <%= link_to "マイページ", user_path(current_user), class: "btn btn-outline-primary form-control" %>
        </div>
        <div>
          <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: "btn btn-outline-danger form-control"%>
        </div>
      <% else %>
        <div class="mx-auto border rounded shadow">
          <div class="border-bottom p-2">
            <%= form_with model: @user, url: user_session_path, class: 'top-session', local: true do |f| %>
              <div class="field m-3">
                <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "メールアドレス", class: "form-control" %>
              </div>
              <div class="field m-3">
                <%= f.password_field :password, autocomplete: "current-password", placeholder: "パスワード", class: "form-control" %>
              </div>
              <div class="actions text-center m-3">
                <%= f.submit "ログイン", class: "btn btn-outline-primary form-control" %>
              </div>
            <% end %>
          </div>
          <div class="d-flex justify-content-around m-2">
            <div class="text-center m-2">
              <%= link_to 'ゲストログイン', users_guest_sign_in_path, class: "btn btn-primary sign_in", method: :post %>
            </div>
            <div class="text-center m-2">
              <%= link_to '新規登録', new_user_registration_path, class: "btn btn-success sign_in" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
<%= render 'about' %>
<%= render 'layouts/footer' %>

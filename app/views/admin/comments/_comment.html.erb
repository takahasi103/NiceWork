<div class="comment-section" style="overflow-y: scroll; max-height: 400px;">
  <% post.comments.each do |comment| %>
    <div class="d-flex m-2">
      <div class="text-center m-2">
        <div>
          <%= image_tag comment.user.get_profile_image, class: "rounded-circle border", size: '50x50' %>
          <br>
          <%= comment.user.name %> <br>
        </div>
      </div>
      <div class="flex-grow-1">
        <div class="says p-3">
          <p>
            <%= comment.body %>
          </p>
          <div class="d-flex justify-content-end">
            <div class="mr-2">
              <%= l comment.created_at %>
            </div>
            <div>
              <%= link_to admin_comment_path(comment), method: :delete, remote: true, data: { confirm: '本当に消しますか？' }, class: "text-danger" do %>
                <i class="fas fa-trash-alt"></i>
              <% end %>
            </div>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>